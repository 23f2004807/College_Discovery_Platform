# Use PostgreSQL (create DB: createdb campus_discovery)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/campus_discovery

# Use SQLite for local dev (no PostgreSQL required):
USE_SQLITE=true

SECRET_KEY=dev_secret_key_change_in_production
JWT_SECRET_KEY=dev_jwt_secret_change_in_production
