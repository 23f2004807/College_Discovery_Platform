# PostgreSQL connection (create database first: createdb campus_discovery)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/campus_discovery

# Set to true only if PostgreSQL is not available (development fallback)
# USE_SQLITE=true

SECRET_KEY=your_secret_key_here
JWT_SECRET_KEY=your_jwt_secret_key_here
